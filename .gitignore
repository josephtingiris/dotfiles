# exclude everyting
/*

# except .ansible.cfg
!.ansible.cfg

# except .bash* files
!.bash_logout
!.bash_profile
!.bashrc
!.bashrc.root

# except .colordiffrc
!.colordiffrc

# except some .config directories
!/.config/
/.config/*
!/.config/nvim

# except .ctags
!.ctags

# except .github directory
!/.github/
!/.github/*

# except .gitignore
!.gitignore

# except .gemrc
!.gemrc

# except some .local directories
!/.local/
/.local/*
!/.local/share
/.local/share/*
!/.local/share/nvim

# except .npmrc
!.npmrc

# except .screenrc
!.screenrc

# except .tmux.conf(s)
!.tmux.conf
!.tmux.conf.2.0-
!.tmux.conf.2.1+
!.tmux.conf.2.9+
!.tmux.conf.3.0+

# except .vimrc
!.vimrc

# except .vim directory
!/.vim/
!/.vim/*
.vim/.netrwhist
.vim/autoload/plug.vim.old
.vim/log
.vim/plug
.vim/shada
.vim/swap
.vim/*tags
#.vim/indent

# except specific files in the bin directory
!/bin/
/bin/*
!/bin/ff
!/bin/fif
!/bin/heic
!/bin/mysql-cp
!/bin/mysql-kill
!/bin/onedrive-sync
!/bin/passwd.master
!/bin/rif
!/bin/rpmnew
!/bin/snmpwalk*
!/bin/scpmv
!/bin/src-dotfiles-rsync
!/bin/test-loop
!/bin/vif
!/bin/windows

# except specific files in the etc/profile.d directory
!/etc/
/etc/restic.conf
/etc/profile.d/*
!/etc/profile.d/README

# except specific files in the opt directory
!/opt/
!/opt/*
.nfs*
